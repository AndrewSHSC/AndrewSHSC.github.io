import * as React from "react";
import * as ReactDOM from "react-dom";
import { App } from "./App";

$(document).ready(() => {

    ReactDOM.render( 

        <App />,
        document.getElementById("what-we-do-app")

    );

});

